type: object
additionalProperties: false
properties:
  name:
    type: string
  type:
    type: string
  extend:
    anyOf:
    - type: array
      items:
        $ref: #
  omit:
    description: List of properties to omit
    type: array
    items:
      type: string
  properties:
    additionalProperties:
      $ref: #
  description:
    description: Description of schema
    type: string
  array:
    description: Wheter schema is array
    type: boolean
  validation:
    $ref: #/definitions/validation
definitions:
  type:
    description: Name of the type
    type: string
  validation:
    type: object
    properties:
      type:
        description: Validation type
        type: string
